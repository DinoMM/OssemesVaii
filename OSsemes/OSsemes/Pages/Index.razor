@page "/"

@using OSsemes.Data
@using OSsemes.Data.Database
@using Microsoft.EntityFrameworkCore


<PageTitle>Hotel X</PageTitle>

<OSsemes.Pages.MainPage.Carousel />

<OSsemes.Pages.MainPage.Content />



@code {
    

    private bool daco = false;
    public void Pridaj()
    {
      /*  var deer = dbContext.Database.GetDbConnection().Database;
        if (dbContext.Database.CanConnect())
        {
            daco = true;
        }*/

      /*  var volaco = dbContext.Zakaznici.ToList();
        Zakaznik novy = new Zakaznik();
        novy.Email = "email@dao.com";
        novy.CisloIzby = 15;
        novy.Priezvisko = "Kovac";
        novy.DatumPrichodu = new DateTime(2023, 11, 10);
        novy.Meno = "Jano";
        novy.PocetDni = 2;
        //novy.ID = 1;
        dbContext.Zakaznici.Add(novy);
        await dbContext.SaveChangesAsync();
        volaco = dbContext.Zakaznici.ToList();*/
    }
}
